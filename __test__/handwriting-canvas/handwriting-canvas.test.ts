import { CanvasManagerInstance } from '../../keyboard/handwriting-canvas/canvas-manager';
import { Canvas } from '../../keyboard/handwriting-canvas/canvas';

describe("handwriting-canvas test", () => {
  it("should correct create two canvas", () => {
    console.log('=============');
    // let button1 = document.createElement('button');
    // let button2 = document.createElement('button');

    // CanvasManagerInstance.register('canvas1', button1);
    // CanvasManagerInstance.register('canvas2', button2);
  });
});

